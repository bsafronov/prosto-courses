---
title: "Итоговая работа: понятная инструкция"
outcomes:
  - show-import
criteria:
  - statement: The documentation shows an import statement without executing it
    outcomes:
      - show-import
time: 25
---

<PracticeTask
  id="practice-task-show-import"
  title="Document an import"
  level="core"
  estimatedMinutes={10}
  goal="Show an import statement as inert documentation"
  outcomes={["show-import"]}
  criteria={["The import remains readable without executing"]}
>
  Add an import example to a short Markdown guide.

  <TaskSolution
    reasoning="A fenced code block preserves the import as documentation."
    alternatives={["Use inline code when the import is the only expression."]}
    likelyErrors={["Writing a live MDX import outside a code fence."]}
  />
</PracticeTask>
