interface:
  display_name: "Course Creation"
  short_description: "Creates and publishes a complete Course"
  default_prompt: "Use $create-course to create and publish a Course from my request."

policy:
  allow_implicit_invocation: false
