interface:
  display_name: "Course Audit"
  short_description: "Audits, fixes, and publishes a Course"
  default_prompt: "Use $audit-course to independently audit and publish a completed Course draft."

policy:
  allow_implicit_invocation: false
