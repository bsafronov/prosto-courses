# Use MDX for lesson sources

Lessons use MDX with a small, platform-owned set of semantic course components. This keeps prose and interactions such as Knowledge Checks together, permits interactions wherever they support the lesson, and avoids separate data files; component inputs are validated during the build so malformed Authoring Agent output fails before deployment.
