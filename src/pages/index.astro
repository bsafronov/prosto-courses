---
import BaseLayout from "../layouts/BaseLayout.astro";
import HomePrototype from "../components/prototype/HomePrototype.astro";
import { getCourses } from "../lib/courses";

const courses = await getCourses();
---
<BaseLayout
  title="Каталог курсов | Prosto.Courses"
  prototype
  prototypePage="home"
>
  <HomePrototype courses={courses} />
</BaseLayout>
